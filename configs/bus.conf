# Bus Configuration
node_id=bus_primary
role=primary

# Network
listen_port=5000
arbiter_host=127.0.0.1
arbiter_port=4000

# Peer bus (secondary, for after demotion)
peer_host=127.0.0.1
peer_port=5001

# Timeouts
heartbeat_interval_ms=2000
failover_timeout_ms=5000